sudo: required
services:
    - docker

before_install:
    - docker build -t jpjongenotter/react-test -f ./client/Dockerfile.dev ./client

script:
    - docker run -e CI=true jpjongenotter/react-test npm test

after_success:
    - docker build -t jpjongenotter/multi-client ./client
    - docker build -t jpjongenotter/multi-nginx ./nginx
    - docker build -t jpjongenotter/multi-server ./server
    - docker build -t jpjongenotter/multi-worker ./worker
    # Log in to the docker CLI
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    # Take those images and push them to docker hub
    - docker push jpjongenotter/multi-client
    - docker push jpjongenotter/multi-nginx
    - docker push jpjongenotter/multi-server
    - docker push jpjongenotter/multi-worker